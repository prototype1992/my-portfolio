import * as React from 'react';

export default class AuthBtn extends React.Component<any, any> {
  public render () {
    return (
      <button className="auth-btn">Авторизоваться</button>
    );
  }
}
